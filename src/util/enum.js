const ENUM_USER_ROLE = {
  USER: "USER",
  HOST: "HOST",
  ADMIN: "ADMIN",
};

const ENUM_TRIP_STATUS = {
  REQUESTED: "requested",
  CANCELED: "canceled",
  ONGOING: "ongoing",
  COMPLETED: "completed",
};

const ENUM_CAR_STATUS = {
  PENDING: "pending",
  APPROVED: "approved",
  CANCELED: "canceled",
};

module.exports = {
  ENUM_USER_ROLE,
  ENUM_TRIP_STATUS,
  ENUM_CAR_STATUS,
};
